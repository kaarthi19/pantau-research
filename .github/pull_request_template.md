<!--
Filing this on the right repo? Personal config — your topics, journals, feeds,
thresholds, prompt — belongs in your own fork, not upstream. See CONTRIBUTING.md.
-->

## What this changes

<!-- One or two sentences. Link the issue if there is one: Fixes #123 -->

## Why

<!-- The behaviour that prompted it. For a bug, what went wrong. -->

## Checklist

- [ ] `make test` passes locally
- [ ] Added or updated a test — and it fails without this change
- [ ] Any dates in test fixtures are relative (`days_ago(2)`), never absolute
- [ ] Still works with **no API key** present (degrades to keyword scoring)
- [ ] A failure introduced here stays contained and doesn't kill a run
- [ ] No new dependency, or the PR explains why one is needed
- [ ] `CHANGELOG.md` updated under `## [Unreleased]` if user-visible

## Anything reviewers should look at closely

<!-- Tradeoffs, things you weren't sure about, alternatives you rejected. -->
