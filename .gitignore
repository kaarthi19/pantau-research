__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
.DS_Store
# NOTE: data/argus.db and docs/index.html ARE committed (by the bot) — do not
# ignore them. Committing state keeps the scheduled workflow alive.

# Your reference library is a PRIVATE local input — never commit it.
library/*.bib
library/*.json
